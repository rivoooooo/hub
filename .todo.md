# SEO Module — Proxy + Abstracted Fetcher

1. **Create abstracted fetcher module (`src/main/fetcher.ts`)**
   - [ ] Export `fetchText(url, options?)` that reads proxy from settings-store
   - [ ] Use `https-proxy-agent` when proxy is enabled

2. **Add proxy fields to `settings-store.ts`**
   - [ ] `proxyEnabled: boolean`, `proxyUrl: string`

3. **Update SEO module to use abstracted fetcher**
   - [ ] Replace raw `fetch()` with `fetchText()`

4. **Update preload types + API**
   - [ ] `index.d.ts` — add proxy fields to SettingsData
   - [ ] `index.ts` — add proxy fields to inline SettingsData

5. **Add proxy UI to settings page**
   - [ ] Toggle + URL input section

6. **Add i18n translations**
   - [ ] `en.json` + `zh-CN.json`

7. **Type check & commit**
   - [ ] `npm run typecheck`
   - [ ] Commit
