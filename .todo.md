# Bridge Module — Tree Structure Enhancement

1. **Phase 1: Refactor Data Types to Recursive Tree Structure**
   - [x] Define new recursive `BridgeNode` type with `object`/`function` differentiation
   - [x] Update `BridgeConfig` to use `tree: BridgeNode[]` with backward-compatible migration
   - [x] Update type definitions in `preload/index.ts` and `preload/index.d.ts`

2. **Phase 2: Create Target‑Page Preload for IPC Channel**
   - [x] Create `src/preload/browser-preload.ts` with `__bridgeCall`
   - [x] Update `electron.vite.config.ts` for two preload entries
   - [x] Wire browser-preload into `browser-manager.ts` for target `WebContentsView`

3. **Phase 3: Rewrite `injectBridge` with Proxy‑Based Recursive Tree**
   - [x] Replace old flat code-gen with Proxy‑based tree injection
   - [x] Add `__bridgeCall` exposure in the existing renderer preload

4. **Phase 4: Main Process IPC Routing (Mode‑A Declarative + Mode‑B Custom)**
   - [x] Register `ipcMain.handle('bridge:call', ...)` in `main/index.ts`
   - [x] Implement mode‑A: declarative match with optional value matching
   - [x] Implement mode‑B: custom function via `new Function()` with try-catch

5. **Phase 5: Update UI Panel to Recursive Tree Editor**
   - [x] Replace flat method list with expandable tree editor
   - [x] Type selector (object / function) per node with conditional fields
   - [x] Update import/export/save for new tree structure
