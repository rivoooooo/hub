# SEO Module — Proxy + Abstracted Fetcher ✅

1. **Create abstracted fetcher module (`src/main/fetcher.ts`)**
   - [x] Export `fetchText(url, options?)` that reads proxy from settings-store
   - [x] Use `https-proxy-agent` when proxy is enabled

2. **Add proxy fields to `settings-store.ts`**
   - [x] `proxyEnabled: boolean`, `proxyUrl: string`

3. **Update SEO module to use abstracted fetcher**
   - [x] Replace raw `fetch()` with `fetchText()`

4. **Update preload types + API**
   - [x] `index.d.ts` — add proxy fields to SettingsData
   - [x] `index.ts` — add proxy fields to inline SettingsData

5. **Add proxy UI to settings page**
   - [x] Toggle + URL input section

6. **Add i18n translations**
   - [x] `en.json` + `zh-CN.json`

7. **Type check & commit**
   - [x] `npm run typecheck` ✅
   - [x] Commit (083e71e)
